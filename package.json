{
  "name": "ticker-cli",
  "version": "1.0.7",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "ticker-cli": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "bun index.ts",
    "build": "bun build --target node index.ts --outdir dist",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "console-table-printer": "^2.15.0",
    "indicatorts": "^2.2.2",
    "yahoo-finance2": "^3.13.0"
  }
}